const Home = () => {
    return (
        <div className="Home">
            <h1>Welcome to the Home Page</h1>
            <button onClick={() => alert('Button clicked!')}>Click Me</button>
        </div>
    );
    }

export default Home;